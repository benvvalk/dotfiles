" hide all toolbars, etc. by default
set gui=none

" don't allow Google search field to automatically grab focus
set focuscontent

" hide/unhide Tree Style Tabs
nmap . :set gui+=tabs<CR>
nmap > :set gui+=notabs<CR>

" speed up scrolling
nnoremap j 3j
nnoremap k 3k
nnoremap <C-y> 3<C-y>
nnoremap <C-e> 3<C-e>

" remap Escape key
noremap <C-c> <Esc>
inoremap <C-c> <Esc>
cnoremap <C-c> <Esc>

" case-insensitive search by default
set ignorecase

" press Ctrl-I to invoke external editor from edit box
set editor="gnome-terminal --disable-factory -x vim"

" tab history extension
" (download from http://vimpr.github.io/plugins-en.html)
nnoremap <S-o> :tabhistory back<CR>
nnoremap <S-i> :tabhistory forward<CR>

highlight Hint font-size:125%;font-weight:bold;color:white;background-color:red;padding:2px;
